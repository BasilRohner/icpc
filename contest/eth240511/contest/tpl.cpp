#include <iostream>
#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef vector<ll> vll;
typedef vector<vll> vvll;
typedef pair<int, int> ii;
typedef vector<ii> vii;

const ll INF = 1e9;
const ll MINF = -1e9;
const ll EPS = 1e-9;
const ll MOD = 1e7+7;

#define rvi(a) for (int i = 0; i < a.size(); i++) scanf("%d",&a[i]); // integer
#define rvl(a) for (int i = 0; i < a.size(); i++) scanf("%lld",&a[i]); // longs
#define rvd(a) for (int i = 0; i < a.size(); i++) scanf("%llf",&a[i]); // doubles

void solve() {
    int k; scanf("%d",&k);
}

int main() {
    int n; scanf("%d",&n);
    for (int i = 0; i < n; i++)
        solve();
}